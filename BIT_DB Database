The following SQL statements are querys focused on a database with multiple tables on customer orders and product quantity.



1.How many many unique orders were placed in January? 

SELECT COUNT(DISTINCT orderid) FROM jansales
WHERE LENGTH(orderid)=6
AND orderid <> 'Order ID';


2.How many of those orders were for an iPhone?

SELECT COUNT(DISTINCT orderid) FROM jansales
WHERE LENGTH(orderid)=6
AND orderid <> 'Order ID'
AND product LIKE 'iPhone';


3.What were the customer account numbers for all the orders placed in feburary?

SELECT DISTINCT acctnum FROM BIT_DB.customers
INNER JOIN febsales
ON customers.order_id = febsales.orderid
WHERE LENGTH(orderid)=6
AND orderid <> 'Order ID';


4.Which product was the cheapest one sold in January, and what was the price?

SELECT DISTINCT product, MIN(price) FROM jansales
WHERE length(orderid) = 6
AND orderid <> 'Order ID'
GROUP BY price ASC LIMIT 1;


5.What is the total revenue for each product sold in January?

SELECT sum(quantity)*price as revenue
,product
FROM BIT_DB.JanSales
GROUP BY product;

6.Which products were sold in February at 548 Lincoln St, Seattle, WA 98101, how many of each were sold, and what was the total revenue?

SELECT Distinct product, SUM(quantity), SUM(quantity)*price as Revenue FROM febsales
WHERE location in
(SELECT location FROM febsales
WHERE location = "548 Lincoln St, Seattle, WA 98101")
GROUP BY product;


7.How many customers ordered more than 2 products at a time in February, and what was the average amount spent for those customers?

SELECT count(acctnum), AVG(quantity*price) FROM febsales
LEFT JOIN customers ON 
febsales.orderid = customers.order_id
WHERE quantity > 2
AND length(orderid)= 6
AND orderid<> 'Order ID';


8. This query shows how many of each type of headphones were sold in February. 

SELECT SUM (quantity), product FROM febsales
WHERE product LIKE "%headphones%"
AND length (Orderid) =6
AND orderid <> "Order ID"
GROUP BY product;


9. Shows how much each customer account spent in the month of February.

SELECT SUM(quantity*price)/ COUNT(customers.acctnum) FROM febsales
LEFT JOIN customers
    ON customers.order_id = febsales.orderid
    WHERE length(orderid) = 6
    AND  orderid <> 'order ID';
    


10. This query shows Order ID's from Georgia who have spent at least 1000 dollars, and where the date starts with '4/1'.

SELECT orderID
FROM BIT_DB.AprSales
WHERE orderdate LIKE '04/1%'
AND location LIKE '%GA%'
AND length(orderid) = 6
AND orderid <> 'Order ID'
GROUP BY orderID
HAVING AVG(price) > 1000;



11. Based off of already given results, this information was reversed engineered to find the query below that would give the same results.

SELECT * FROM maysales
WHERE orderid LIKE '1943_5'
AND length(orderid) = 6
AND orderid <> 'Order ID'
ORDER BY orderID;



12. The query below shows which product in January brought in the most revenue.

SELECT product, SUM(quantity*price) FROM jansales
WHERE LENGTH (orderid) = 6
AND orderid <> 'Order id'
GROUP BY product
ORDER BY SUM(quantity*price) DESC
LIMIT 1

